% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_functions.R
\name{optimize_delays}
\alias{optimize_delays}
\title{Méta-fonction d'optimisation des délais}
\usage{
optimize_delays(
  data,
  type = "both",
  method = "delay_adjustment",
  delay_range = -25:25,
  n_iterations = 10,
  noise_level = 0.03,
  apply_correction = TRUE
)
}
\arguments{
\item{data}{Tibble avec données de rendement}

\item{type}{Type de délai à optimiser: "flow" (flux), "moisture" (humidité),
ou "both" (les deux). Défaut: "both"}

\item{method}{Méthode d'optimisation. Défaut: "delay_adjustment"}

\item{delay_range}{Plage de délais à tester en secondes. Défaut: -25:25}

\item{n_iterations}{Nombre d'itérations pour la stabilité. Défaut: 10}

\item{noise_level}{Niveau de bruit ajouté. Défaut: 0.03}

\item{apply_correction}{Si TRUE, applique la correction de délai aux données.
Défaut: TRUE}
}
\value{
Liste contenant:
\itemize{
\item data: Données corrigées (si apply_correction = TRUE)
\item delays: Délai(s) optimal(aux) trouvé(s)
\item delay_adjustment_results: Résultats détaillés de l'optimisation
}
}
\description{
Cette fonction optimise les délais temporels entre les capteurs (flux et humidité)
en utilisant la méthode de delay adjustment (Delay Adjustment).
}
\examples{
\dontrun{
# Optimiser les deux délais et appliquer les corrections
result <- optimize_delays(data, type = "both")
data_corrected <- result$data

# Optimiser uniquement le délai de flux sans appliquer
result <- optimize_delays(data, type = "flow", apply_correction = FALSE)
print(result$delays$flow)
}
}
