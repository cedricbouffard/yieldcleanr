% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_functions.R
\name{export_data}
\alias{export_data}
\title{Méta-fonction d'export des données}
\usage{
export_data(data, file, format = NULL, ...)
}
\arguments{
\item{data}{Données à exporter (tibble ou objet sf)}

\item{file}{Chemin du fichier de sortie}

\item{format}{Format d'export: "csv", "geojson", "shp", "gpkg", "raster".
Si NULL, détecté automatiquement à partir de l'extension du fichier.}

\item{...}{Paramètres supplémentaires pour l'export}
}
\value{
Chemin du fichier créé (invisible)
}
\description{
Exporte les données nettoyées dans différents formats (CSV, GeoJSON, Shapefile,
raster, etc.).
}
\examples{
\dontrun{
# Export CSV
export_data(data, "output.csv", format = "csv")

# Export GeoJSON (format auto-détecté)
export_data(data, "output.geojson")

# Export Shapefile avec options
export_data(data, "output.shp", format = "shp", overwrite = TRUE)

# Export raster
export_data(data, "yield.tif", format = "raster", resolution = 5)
}
}
