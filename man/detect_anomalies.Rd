% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_functions.R
\name{detect_anomalies}
\alias{detect_anomalies}
\title{Méta-fonction de détection d'anomalies}
\usage{
detect_anomalies(data, type = "all", action = "filter", ...)
}
\arguments{
\item{data}{Tibble avec données de rendement}

\item{type}{Type d'anomalie à détecter. Peut être un vecteur. Options:
"overlap" (chevauchement), "local_sd" (écart-type local), "velocity_jump"
(changement de vitesse), "heading" (direction), "position" (position),
"all" (toutes)}

\item{action}{Action à effectuer: "filter" (filtrer les anomalies), "detect"
(marquer sans filtrer), ou "report" (rapport uniquement). Défaut: "filter"}

\item{...}{Paramètres spécifiques au type d'anomalie:
\itemize{
\item overlap: cellsize (défaut: 0.3), max_pass (défaut: 50)
\item local_sd: n_swaths (défaut: 5), lsd_limit (défaut: 2.4), min_cells (défaut: 3)
\item velocity_jump: max_acceleration (défaut: 5), max_deceleration (défaut: -8)
\item heading: max_heading_change (défaut: 60)
\item position: gbuffer (défaut: 100)
}}
}
\value{
Selon l'action: data filtré, data avec colonnes de marquage, ou rapport
}
\description{
Cette fonction unifiée détecte et filtre différents types d'anomalies dans les
données de rendement : chevauchements, écarts-types locaux, changements brusques
de vitesse, anomalies de direction et points hors champ.
}
\examples{
\dontrun{
# Détecter et filtrer toutes les anomalies
data_clean <- detect_anomalies(data, type = "all")

# Détecter uniquement les chevauchements avec paramètres personnalisés
data_clean <- detect_anomalies(data, type = "overlap",
                               cellsize = 0.5, max_pass = 30)

# Marquer sans filtrer
data_marked <- detect_anomalies(data, type = c("overlap", "local_sd"),
                                action = "detect")
}
}
